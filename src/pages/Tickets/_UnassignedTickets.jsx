import React from "react";
import styled from "styled-components";

const Container = styled.div``;

const UnassignedTickets = () => {
  return <Container>Unassigned Tickets</Container>;
};

export default UnassignedTickets;
