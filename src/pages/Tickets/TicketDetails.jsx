import React from "react";
import styled from "styled-components";
// import { useParams } from "react-router-dom";

const Container = styled.div``;

const TicketDetails = () => {
  // const { id } = useParams();

  return <Container>Ticket Details</Container>;
};

export default TicketDetails;
